# Backend Configuration
# ====================

# Backend Port
# Port for the NestJS backend API
BACKEND_PORT=3000

# Node Environment
# Options: development, production, test
NODE_ENV=production

# Contracts Configuration
# =======================

# Contracts Path
# Glob pattern for YAML contract files (e.g., '/contracts/**/*.yml' or '/path/to/contracts/*.yaml')
CONTRACTS_PATH=/contracts/**/*.yml

# Neo4j Configuration
# ===================

# Neo4j Authentication
# Default user is 'neo4j', change password for security
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-secure-password-here

# Neo4j Ports
# HTTP port for Neo4j Browser access
NEO4J_HTTP_PORT=7474
# Bolt port for database connections
NEO4J_BOLT_PORT=7687

# Neo4j Plugins
# List of plugins to install (e.g., '["apoc"]' or '["apoc", "graph-data-science"]')
NEO4J_PLUGINS='["apoc"]'

# Neo4j Memory Configuration
# Initial heap size
NEO4J_HEAP_INITIAL_SIZE=512M
# Maximum heap size
NEO4J_HEAP_MAX_SIZE=2G
# Page cache size (recommended: ~50% of RAM dedicated to Neo4j)
NEO4J_PAGECACHE_SIZE=512M

# Procedures Configuration
# Unrestricted procedures (comma-separated, use with caution)
NEO4J_UNRESTRICTED_PROCEDURES=apoc.*
