# Backend Configuration
# ====================

# Backend Port
# Port for the NestJS backend API
BACKEND_PORT=3000

# Node Environment
# Options: development, production, test
# Note: Set to 'development' to enable test watch mode with Docker
# Use: docker-compose --profile development up backend-test-watch
NODE_ENV=production

# Contracts Configuration
# =======================

# Contracts Host Directory (Docker volume mount)
# Path to contracts directory on HOST machine - MUST be a directory path (no glob pattern)
# This directory will be mounted into the Docker container at /contracts
# Examples: './contracts', './my-contracts', '/absolute/path/to/contracts'
CONTRACTS_HOST_DIR=./contracts

# Contracts Path (Application glob pattern)
# Glob pattern used by the application to find YAML files INSIDE the container
# This is the pattern the backend will use to search for contract files
# Examples: '/contracts/**/*.yml', '/contracts/*.yaml', '/contracts/services/**/*.yml'
CONTRACTS_PATH=/contracts/**/*.yml

# Neo4j Configuration
# ===================

# Neo4j Authentication
# Default user is 'neo4j', change password for security
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-secure-password-here

# Neo4j Ports
# HTTP port for Neo4j Browser access
NEO4J_HTTP_PORT=7474
# Bolt port for database connections
NEO4J_BOLT_PORT=7687

# Neo4j Plugins
# List of plugins to install (e.g., '["apoc"]' or '["apoc", "graph-data-science"]')
NEO4J_PLUGINS='["apoc"]'

# Neo4j Memory Configuration
# Initial heap size
NEO4J_HEAP_INITIAL_SIZE=512M
# Maximum heap size
NEO4J_HEAP_MAX_SIZE=2G
# Page cache size (recommended: ~50% of RAM dedicated to Neo4j)
NEO4J_PAGECACHE_SIZE=512M

# Procedures Configuration
# Unrestricted procedures (comma-separated, use with caution)
NEO4J_UNRESTRICTED_PROCEDURES=apoc.*

# HuggingFace Configuration
# =========================

# HuggingFace Access Token
# Required for downloading gated models like google/embeddinggemma-300m
# Get your token at: https://huggingface.co/settings/tokens
# Steps:
# 1. Create a HuggingFace account at https://huggingface.co
# 2. Go to https://huggingface.co/settings/tokens
# 3. Create a new token (read access is sufficient)
# 4. Accept the model terms at: https://huggingface.co/google/embeddinggemma-300m
# 5. Paste your token below
HF_TOKEN=your-huggingface-token-here
