# Development Dockerfile with hot reload support
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm ci

# Copy source code
COPY . .

# Expose the application port
EXPOSE 3000

# Run the application in watch mode for auto-reload
CMD ["npm", "run", "start:dev"]
